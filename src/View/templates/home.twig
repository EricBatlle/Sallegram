{% extends "base.twig" %}
{% block links %}
    {{ parent() }}
    {% if ( logged ) %}
        <a href="/addImg">Add Image</a>
    {% endif %}


{% endblock %}

{% block content %}
    <h1>Home</h1>

    <h2>Top5</h2>
    {% for i in 1..top5|length %}
        <div>
            <h3>Img {{ top5[i-1].img_path }}</h3>
            <img src="assets/uploads/{{ top5[i-1].img_path }}">
            <p>Title {{ top5[i-1].title }}</p>
            {#ToDo: Change user_id to name#}
            <p>UserName {{ top5[i-1].user_id }}</p>
            <p>Date {{ top5[i-1].created_at }}</p>
            <p>Likes {{ top5[i-1].likes }}</p>
            <p>Visits {{ top5[i-1].visits }}</p>


           {# <p><?php echo $posts[$i]['created_at']?></p>
            <!--<img src="http://www.kaczmarek-photo.com/wp-content/uploads/2012/06/guinnes-150x150.jpg" alt="post img" class="pull-left img-responsive thumb margin10 img-thumbnail">-->
            <article><p>
                    <?php echo $posts[$i]['content']?>
                </p></article>
            <a class="btn btn-blog pull-right marginBottom10"><?php echo $posts[$i]['username']?></a>
        </div>
        <?php } ?>
        <div class="col-md-12 gap10"></div>#}
        </div>
    {% endfor %}

{% endblock %}